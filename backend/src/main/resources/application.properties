spring.application.name=sep-backend

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:sep}
spring.datasource.username=${DB_USERNAME:sep}
spring.datasource.password=${DB_PASSWORD:sepsep123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
